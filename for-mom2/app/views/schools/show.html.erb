<h1>students applying to <%= @school.name %>:</h1>

<% if @school.users.length > 0 %>
  <ul>
    <% @school.users.each do |user| %>
      <li><%= link_to user.name, user_path(user) %></li>
    <% end %>
  </ul>
<% else %>
  <p>
    No one is applying here yet!
  </p>
<% end %>
